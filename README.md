# Glocalize

One repo to bring them all and in the darkness bind them...

## Setup

1. Put the repos and services as subdirectories to this repo (on your local machine).
2. Ignore them in `.gitignore`.
3. Add them to `docker-compose.yml`

## Start

```
docker-compose up
```
